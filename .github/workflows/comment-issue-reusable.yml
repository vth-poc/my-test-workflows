name: Reusable Comment on Issue

on:
  workflow_call:
    inputs:
      owner:
        required: true
        type: string
      repo:
        required: true
        type: string
      issue_number:
        required: true
        type: string
      user_login:
        required: true
        type: string
      body:
        required: false
        type: string

permissions:
  issues: write

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v8
        with:
          script: |
            const body = "${{ inputs.body }}" || `Hi @${{ inputs.user_login }},\n\n👋 Thanks for opening this issue!`;
            github.rest.issues.createComment({
              issue_number: "${{ inputs.issue_number }}",
              owner: "${{ inputs.owner }}",
              repo: "${{ inputs.repo }}",
              body: body
            })
